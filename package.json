{
  "name": "pomodorise",
  "version": "1.0.0",
  "private": true,
  "description": "PomodoRise - Full Stack Pomodoro Timer con gamificación",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "clean": "pnpm -r run clean && rm -rf node_modules"
  },
  "keywords": [
    "pomodoro",
    "productivity",
    "gamification",
    "typescript",
    "monorepo"
  ],
  "author": "Alejandro Goscu",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.19.1"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/jsonwebtoken": "^9.0.10"
  }
}
